/**
 * Shared /api/bootstrap react-query key (official Go account residual).
 * App login-gate seeds this; Code Tw CodeGreeting + settings read the same cache.
 */
export const BOOTSTRAP_QUERY_KEY = ["api", "bootstrap"] as const;
