const React = typeof window !== "undefined" ? window.React : {};
export default React;
export const {
  useRef,
  useImperativeHandle,
  useEffect,
  useContext,
  useState,
  useCallback,
  useMemo,
  useLayoutEffect,
  createContext,
  createElement,
  forwardRef,
  memo,
  Fragment,
  Children,
  Component,
  PureComponent,
  cloneElement,
  isValidElement,
  createRef,
} = React;
